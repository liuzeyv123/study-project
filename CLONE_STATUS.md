# Komari clone status

I attempted to clone `https://github.com/sasanquaa/komari.git` into this repository from the current environment, but outbound GitHub HTTPS requests are blocked with:

`CONNECT tunnel failed, response 403`

Use `scripts/import_komari.sh` in a network environment that can access GitHub to import the repository contents into this repo.
